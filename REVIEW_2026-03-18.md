# 代码库 Review 记录（2026-03-18）

范围：`scripts/`、`scenes/`、`data/operators/` 全量审查，重点覆盖射击链路、状态系统、UI 与性能风险。

## 发现的问题

1. `[P1]` 热路径日志写盘会导致明显卡顿  
位置：`scripts/core/debug_log.gd:20-27,35-41`，`scripts/combat/weapon_shooter.gd:44,53,77,92,113,117`  
现状：`DebugLog.add_entry()` 每次调用都会同步重写 `user://debug.log`。而开火路径上日志非常密集（每次开火、每次命中/未命中都写），会把 IO 压力直接放到帧循环里。  
影响：在高射速/多弹体时容易出现帧率骤降和抖动，和“帧数爆跌”现象一致。  

2. `[P2]` 弹道 Trail 每发会创建大量临时 3D 节点，分配/回收开销偏高  
位置：`scripts/combat/weapon_shooter.gd:160-204`  
现状：`_spawn_tracer()` 按 `pellet_count * tracer_segments` 创建 `MeshInstance3D + CylinderMesh + StandardMaterial3D + Tween`，并在短生命周期后释放。  
影响：算子放大弹体数量后，节点创建量线性暴增，易触发 GC/渲染开销尖峰。  

3. `[P2]` 命中特效为“按 tag 实时创建粒子节点”，缺少总量控制  
位置：`scripts/combat/target_dummy.gd:112-127,270-283`  
现状：每次命中按 tag 触发 `_spawn_effect_burst()`，每次都创建新的 `GPUParticles3D` 与 `Timer`。虽有每 tag 0.06s 的去抖，但没有全局上限。  
影响：高频命中或多目标情况下，会叠加较多瞬时节点，带来 CPU 和渲染波动。  

4. `[P3]` 开火入口双路径并存，维护复杂度和行为一致性风险偏高  
位置：`scripts/combat/weapon_shooter.gd:40-55`  
现状：`_process()` 与 `_unhandled_input()` 都会触发 `fire()`，依赖 `_next_fire_time` 防重复。  
影响：功能目前可用，但行为分散，后续修改射击时序时更易引入边界问题（比如首次点击响应、长按逻辑、日志节流的一致性）。  

## 修复优先级建议

1. 先处理 `[P1]`：热路径日志降噪 + 写盘频率控制。  
2. 再处理 `[P2]`：限制/池化弹道与命中特效节点。  
3. 最后处理 `[P3]`：统一开火入口，降低后续迭代风险。  

